#!/bin/bash

# Создаем директорию для хранения конфигурации
mkdir -p wg-config

# Базовый конфиг будет создан автоматически при первом запуске API
# Docker Compose монтирует том wg-config в контейнер
